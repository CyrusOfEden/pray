<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />

    <title>Upcoming</title>

    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <link rel="apple-touch-icon" href="/levitation.png">

    <%= stylesheet_link_tag "app" %>
  </head>
  <body data-ng-app="Pray" data-ng-controller="AppCtrl" data-ng-init="start()" data-ng-class="{ show: ready }">

    <nav data-ng-click="state = state == 'upcoming' ? 'forecast' : 'upcoming'" data-ng-class="state"></nav>
    <main>
      <h1>&#xfdfd;</h1>
      <section class="upcoming" data-ng-class="{ show: state == 'upcoming' }">
        <p data-ng-repeat="prayer in prayers track by $index" data-ng-class="{ current: prayer.current() }">
          <strong>{{ prayer.name.capitalize() }}</strong> {{ relative(prayer) }}
        </p>
      </section>
      <section class="forecast" data-ng-class="{ show: state == 'forecast' }">

      </section>
      <h2 data-ng-style="bearing">&#10148;</h2>
    </main>

    <script src="//ajax.googleapis.com/ajax/libs/angularjs/1.2.13/angular.min.js"></script>
    <%= javascript_include_tag "app" %>
  </body>
</html>